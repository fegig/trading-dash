/** Pending manual wallet ops — must match inserts and admin filters. */
export const WALLET_METHOD_WITHDRAWAL_REQUEST = 'Withdrawal request'
export const WALLET_METHOD_DEPOSIT_REQUEST = 'Deposit request'

/** Deposit intents expire after 1 hour (seconds). */
export const DEPOSIT_INTENT_TTL_SEC = 3600
